services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
    - 8000:80      # Map local port 8000 to container port 80
    environment:
    - REDIS_HOST=host.docker.internal
    - REDIS_PORT=6379
    - APP_ENV=production
    - LOG_LEVEL=INFO
    healthcheck:
      test: [CMD, curl, -f, http://localhost:80/health]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s
    restart: unless-stopped
